% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/khan_realdata.R
\name{khan_realdata}
\alias{khan_realdata}
\title{Run the full KHAN pipeline on real data}
\usage{
khan_realdata(
  X = NULL,
  sigma_hat = NULL,
  theta_hat = NULL,
  n = NULL,
  q = 0.1,
  mu_range = c(0, Inf),
  scenario = "a",
  k = 1L,
  ...
)
}
\arguments{
\item{X}{Optional \code{n}-by-\code{d} data matrix. If supplied, the
sample covariance is used and \code{\link[=estimate_ggm]{estimate_ggm()}}
fits the GLasso.}
\item{sigma_hat}{Covariance estimate (required if \code{X} is \code{NULL}).}
\item{theta_hat}{Precision estimate (required if \code{X} is \code{NULL}).}
\item{n}{Sample size; inferred from \code{X} when possible.}
\item{q}{FDR level.}
\item{mu_range}{Filtration interval.}
\item{scenario}{\code{"a"} (two-sided / GGM) or \code{"b"} (one-sided).}
\item{k}{Homology dimension.}
\item{...}{Additional arguments forwarded to
\code{\link[=estimate_ggm]{estimate_ggm()}}.}
}
\value{
A list combining the \code{\link[=khan]{khan()}} result with the
intermediate \code{theta_d}, \code{var_mat}, and \code{emap} so users can
inspect or reuse them.
}
\description{
Given a dataset or a GLasso estimate, this function: (1) estimates the
precision matrix (if only \code{X} is provided), (2) computes the
debiased estimator via \code{\link[=debias_glasso]{debias_glasso()}},
(3) estimates edge variances via
\code{\link[=estimate_variance_ggm]{estimate_variance_ggm()}}, and
(4) runs \code{\link[=khan]{khan()}} for persistent-homology selection.
}

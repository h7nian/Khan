% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/khan.R
\name{khan}
\alias{khan}
\title{KHAN algorithm for persistent homology selection (Algorithm 3)}
\usage{
khan(
  w_hat,
  sigma_e,
  n,
  emap,
  d,
  q,
  mu_range = c(0, Inf),
  scenario = "a",
  k = 1L
)
}
\arguments{
\item{w_hat}{Edge-weight estimates (numeric vector, length \code{n_edges}).}
\item{sigma_e}{Edge standard deviations (numeric vector).}
\item{n}{Sample size.}
\item{emap}{Edge-index map.}
\item{d}{Number of nodes.}
\item{q}{FDR level.}
\item{mu_range}{Numeric length-2 vector \code{c(mu_0, mu_1)}; use
\code{c(0, Inf)} for unbounded tails.}
\item{scenario}{\code{"a"} (two-sided / GGM) or \code{"b"} (one-sided /
Ising).}
\item{k}{Homology dimension (1 or 2).}
}
\value{
List with:
\describe{
  \item{\code{barcode}}{Data frame with columns \code{generator_id},
    \code{birth_mu}, \code{death_mu}.}
  \item{\code{change_points}}{Numeric vector of change points
    \code{mu^{(t)}}.}
  \item{\code{selected_by_mu}}{Function \code{mu -> dgs()} result at the
    last change point \code{<= mu}.}
  \item{\code{results_at_changes}}{List of DGS results, one per change
    point.}
}
}
\description{
Iteratively locates change points on the filtration line and applies DGS
at each change point, providing uFDR-controlled persistent homology
selection (Section 3.2 of the paper).
}

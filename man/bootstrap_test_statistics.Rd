% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_test_statistics}
\alias{bootstrap_test_statistics}
\title{Gaussian multiplier bootstrap test statistics}
\usage{
bootstrap_test_statistics(X, theta_hat, sigma_hat, emap, num_B = 1000L)
}
\arguments{
\item{X}{n-by-d data matrix.}
\item{theta_hat}{Estimated precision matrix.}
\item{sigma_hat}{Estimated (or sample) covariance matrix; currently unused
but retained for interface symmetry with other estimators that may need
it.}
\item{emap}{Edge-index map.}
\item{num_B}{Number of bootstrap replicates.}
}
\value{
Numeric matrix of dimension \code{num_B} by \code{d*(d-1)/2}.
}
\description{
For each bootstrap replicate \code{b = 1..num_B} and each edge \code{e}:
\code{T^{(b)}_e = (1 / sqrt(n)) sum_i g_i * xi_i(e) / sigma_e} where
\code{g_i ~ N(0, 1)}, \code{xi_i(e)} are the GGM influence-function
summands, and \code{sigma_e} is the empirical standard deviation of
\code{xi_.(e)}.
}

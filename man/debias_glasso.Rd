% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggm.R
\name{debias_glasso}
\alias{debias_glasso}
\title{Debiased GLasso estimator (equation 2.7)}
\usage{
debias_glasso(sigma_hat, theta_hat)
}
\arguments{
\item{sigma_hat}{Estimated (or sample) covariance matrix.}
\item{theta_hat}{Estimated precision matrix.}
}
\value{
Debiased precision matrix (d-by-d).
}
\description{
Computes
\deqn{\hat{\Theta}^d_{uv} = \hat{\Theta}_{uv} -
  \hat{\Theta}_{u \cdot}^\top
  (\hat{\Sigma} \hat{\Theta}_{\cdot v} - e_v) /
  (\hat{\Theta}_{u \cdot}^\top \hat{\Sigma}_{\cdot u}).}
Uses matrix products (\code{theta_hat \%*\% sigma_hat},
\code{sigma_hat \%*\% theta_hat}) so the per-entry work is a dot product
rather than the nested scalar loop in the original implementation.
}
